2022-02-24

키보드 인풋 외우기

keyboard input는 총 세 줄을 적어야 함
		// 키보드 객체 생성
		Scanner keyboard = new Scanner(System.in);
		
		// 입력 데이터 맞는 변수 준비해서 데이터를 받음
		System.out.println("정수 값(10~20)을 입력하세요 :");
		String input = keyboard.nextLine(); // => 문자도 가능
		// int input = keyboard.nextInt(); // => 숫자만


void Main은 하나는 필요하고 2개는 쓰일 수 없다

이름() => 메서드

패키지가 공간
패키지 밑의 클래스들은 다 같은 공간에 모여 있는 것

Public 멤버는 class 외부에서 접근할 수 있는 변수를 의미 > 외부공개 (완전공개)
Private 멤버는 class 외부에서 접근할 수 없는 변수를 의미 > 내부전용 (비공개)
protected 는 class 상관없이 호출해주면 사용이 가능함 > (선택적공개)
static => 클래스에서 변수처럼 호출하여 사용이 가능하다???? 이해안됨;

최상위 클래스 = 오브젝트
모든 상위 클래스 = 슈퍼클래스


생성자
메서드
메인
맨 밑에 투스트링
_________________________________________________________

1. 클래스 기본
  - 멤버, 속성, 상태 <=== 데이터 (변수, 배열, 상수*변하지 않는 수)
  - 메서드*알고리즘 (기능, 행위) <=== 멤버 상태를 변경

2. 생성자
  - 오버로딩
    같은 이름을 가진 여러개의 메서드
    (ex. print)

3. 상속 (extend = copy + x)
  - 오버라이딩
  - static ??? 멤버, 메서드 (공유 기능 제공)

4. 추상클래스

5. 인터페이스
  - 객체 생성이 목표가 아님
  - ~의 규격을 제시 , 프로젝트할때 가장 먼저 해야할 일


static 이란
객체간의 공유를 위한 값을 설정하는 것
static int numOfCars;
사용할때는 Cars.numOfCars= @; 사용할때에는 값 변경 가능


*추상클래스는 상속해서 사용

*인터페이스 interface : 기능만 호출
      = 정해진 동일한 규격이 있기에 그 규격대로 제품을 만드는데에 사용됨
 implements : 기능을 구현 + 인터페이스와 함께 사용

_________________________________________________________


클래스 앞의 public 이란 '상속 가능하게' 라는 뜻
public class @@@{ 

}


super.클래스명 => 상위 클래스를 사용할 수 있게 된다 

청년복지정책
청년형 소득공제 장기펀드
무주택 청년 주거지원
청년 내일저축
