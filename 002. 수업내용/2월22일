2022-02-22

좌측 빈칸에 우클릭>New>Project
ControlPrj
JRE 부분에 JavaSE-1.8 되어 있는지 확인하기
finish

src 밑에 class만들기
Package : com.simple.ifex
Name : IfEx01
public static void main 체크 확인

keyboard input는 총 세 줄을 적어야 함
		// 키보드 객체 생성
		Scanner keyboard = new Scanner(System.in);
		
		// 입력 데이터 맞는 변수 준비해서 데이터를 받음
		System.out.println("정수 값(10~20)을 입력하세요 :");
		String input = keyboard.nextLine(); // => 문자도 가능
		// int input = keyboard.nextInt(); // => 숫자만


지역변수
전역변수


난수
%d 10진수(정수형)
%s 문자열
%f (%6.2f란?)
printf => 출력될 데이터(" ")내부에 변수를 내 임의대로 지정 가능
			// 성적점수 1~100을 생성한다.
			// rand = (int) (Math.random() * (MAX-MIN+1))+MIN; *공식처럼 외우기

